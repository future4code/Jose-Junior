export type user = {
    id: string;
    name: string;
    email:string;
    password: string;
}

export type recipe = {
    id: string;
    title: string;
    descripstion: string;
    date: string;
}